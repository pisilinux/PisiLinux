diff -Nuar qtermwidget-pyqt-fixes.orig/pyqt4/qtermwidget.sip qtermwidget-pyqt-fixes/pyqt4/qtermwidget.sip
--- qtermwidget-pyqt-fixes.orig/pyqt4/qtermwidget.sip	2012-12-29 00:29:44.000000000 +0200
+++ qtermwidget-pyqt-fixes/pyqt4/qtermwidget.sip	2012-12-29 04:11:43.263435431 +0200
@@ -23,7 +23,7 @@
     void setShellProgram(const QString & progname);
 	void setArgs(QStringList &args);
 	void setTextCodec(QTextCodec *codec);
-	void setColorScheme(const char *scheme);
+	//void setColorScheme(const char *scheme);
 	void setSize(int h, int v);
 	void setHistorySize(int lines);
 	void setScrollBarPosition(ScrollBarPosition);
@@ -60,8 +60,8 @@
     
 
     //Color scheme, default is white on black
-    void setColorScheme(const QString & name);
-    static QStringList availableColorSchemes();
+    //void setColorScheme(const QString & name);
+    //static QStringList availableColorSchemes();
 
     // Wrapped, scroll to end.
     void scrollToEnd();
