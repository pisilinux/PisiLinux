diff -uNBwp src/qsvnactions/CMakeLists.txt.orig src/qsvnactions/CMakeLists.txt
--- src/qsvnactions/CMakeLists.txt.orig	1970-01-01 01:00:00.000000000 +0100
+++ src/qsvnactions/CMakeLists.txt	2011-02-07 13:17:34.728697806 +0100
@@ -0,0 +1,2 @@
+#it is here just to create the subdirectory during build
+
diff -uNBwp src/CMakeLists.txt.orig src/CMakeLists.txt
--- src/CMakeLists.txt.orig	2011-02-07 13:16:04.165438805 +0100
+++ src/CMakeLists.txt	2011-02-07 13:20:15.687138755 +0100
@@ -1,5 +1,7 @@
 PROJECT(qsvn)
 
+OPTION ( BUILD_TESTS "Build test programs" ON )
+
 CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
 
 #Set Policy for cmake version 2.6.0 and above
@@ -12,10 +14,10 @@ INCLUDE (CheckCXXCompilerFlag)
 SET(QSVN_MAJOR_VERSION 0)
 SET(QSVN_MINOR_VERSION 8)
 SET(QSVN_MICRO_VERSION 90)
-SET(QSVN_BUILD_VERSION 0)
+SET(QSVN_BUILD_VERSION 1127)
 
-SET(QSVN_VERSION_STRING "${QSVN_MAJOR_VERSION}.${QSVN_MINOR_VERSION}.${QSVN_MICRO_VERSION}")
-SET(QSVN_VERSION_NUMBER ${QSVN_MAJOR_VERSION}.${QSVN_MINOR_VERSION}.${QSVN_MICRO_VERSION})
+SET(QSVN_VERSION_STRING "${QSVN_MAJOR_VERSION}.${QSVN_MINOR_VERSION}.${QSVN_MICRO_VERSION}.${QSVN_BUILD_VERSION}")
+SET(QSVN_VERSION_NUMBER ${QSVN_MAJOR_VERSION}.${QSVN_MINOR_VERSION}.${QSVN_MICRO_VERSION}.${QSVN_BUILD_VERSION})
 
 IF (UNIX)
 CHECK_CXX_COMPILER_FLAG(-fvisibility=hidden HAVE_GCC_VISIBILITY)
@@ -47,11 +49,10 @@ IF (WIN32)
   SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR}/bin)
   SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR}/bin)
 ELSE (WIN32)
-  IF(CMAKE_SIZEOF_VOID_P EQUAL 4)
     SET(LIB_SUFFIX "")
-  ELSE(CMAKE_SIZEOF_VOID_P EQUAL 4)
+  IF((CMAKE_SIZEOF_VOID_P EQUAL 4) AND (EXISTS ${CMAKE_INSTALL_PREFIX}/lib64))
     SET(LIB_SUFFIX 64)
-  ENDIF(CMAKE_SIZEOF_VOID_P EQUAL 4)
+  ENDIF((CMAKE_SIZEOF_VOID_P EQUAL 4) AND (EXISTS ${CMAKE_INSTALL_PREFIX}/lib64))
   SET(LIB_INSTALL_DIR ${CMAKE_INSTALL_PREFIX}/lib${LIB_SUFFIX})
   SET(BIN_INSTALL_DIR ${CMAKE_INSTALL_PREFIX}/bin)
   SET(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR}/lib${LIB_SUFFIX})
@@ -74,6 +75,7 @@ INCLUDE_DIRECTORIES(
 )
 
 ADD_SUBDIRECTORY(svnqt)
+ADD_SUBDIRECTORY(qsvnactions)
 
 SET(qsvn_headers
          checkout.h
@@ -194,6 +196,8 @@ IF (UNIX)
     INSTALL(TARGETS qsvn
       RUNTIME DESTINATION ${BIN_INSTALL_DIR}
       LIBRARY DESTINATION ${LIB_INSTALL_DIR})
+    INSTALL(FILES qsvn.desktop DESTINATION ${CMAKE_INSTALL_PREFIX}/share/applications)
+    INSTALL(FILES images/qsvn.png DESTINATION ${CMAKE_INSTALL_PREFIX}/share/pixmaps)
 ENDIF (UNIX)
 
 IF (WIN32)
@@ -212,6 +216,8 @@ IF (WIN32)
 ENDIF (WIN32)
 
 # Unit-Tests
+IF (BUILD_TESTS)
 ENABLE_TESTING()
 ADD_SUBDIRECTORY(tests)
 ADD_SUBDIRECTORY(qsvnactions/tests)
+ENDIF (BUILD_TESTS)
diff -uNBwp src/svnqt/CMakeLists.txt.orig src/svnqt/CMakeLists.txt
--- src/svnqt/CMakeLists.txt.orig	2011-02-07 13:16:04.190451305 +0100
+++ src/svnqt/CMakeLists.txt	2011-02-07 13:18:26.049344168 +0100
@@ -18,6 +18,7 @@
  #   51 Franklin Street, Fifth Floor, Boston, MA  02110-1301, USA.         #
  ####
 
+OPTION ( BUILD_TRANSLATIONS "Build translations (incomplete for now)" OFF )
 include (SvnqtMacros)
 
 SET(SOURCES
@@ -150,7 +151,7 @@ IF (WIN32)
     SET(_SVNQT_EXPORT "__declspec(dllexport)")
 ENDIF (WIN32)
 
-IF (QT_LUPDATE_EXECUTABLE)
+IF (BUILD_TRANSLATIONS AND QT_LUPDATE_EXECUTABLE)
     MESSAGE(STATUS "Setting up language target")
 
     FILE(GLOB _LANGFILES lang/*.ts)
@@ -168,7 +169,7 @@ IF (QT_LUPDATE_EXECUTABLE)
         VERBATIM
     )
     ADD_SUBDIRECTORY(lang)
-ENDIF (QT_LUPDATE_EXECUTABLE)
+ENDIF (BUILD_TRANSLATIONS AND QT_LUPDATE_EXECUTABLE)
 
 CONFIGURE_FILE(
     "${CMAKE_CURRENT_SOURCE_DIR}/svnqt_defines.h.in"
