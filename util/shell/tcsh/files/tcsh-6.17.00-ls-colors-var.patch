--- tw.color.c	2008-10-17 21:57:33.000000000 +0200
+++ tw.color.c.ls-colors	2010-03-01 09:46:18.000000000 +0100
@@ -86,6 +86,10 @@
     VAR(NOS, "ow", ""),		/* Other writable dir (o+w) but not sticky */
     VAR(NOS, "st", ""),		/* Sticky dir (+t) but not other writable */
     VAR(NOS, "rs", "0"),	/* Reset to normal color */
+    VAR(NOS, "hl", ""),                /* Regular file with more than one link */
+    VAR(NOS, "ca", ""),                /* File with capability */
+    VAR(NOS, "mh", ""),                /* MULTIHARDLINK */
+    VAR(NOS, "cl", ""),                /* CLRTOEOL */
 };
 
 enum FileType {
