--- a/guilt	
+++ a/guilt	
@@ -33,6 +33,7 @@ case "$gitver" in
 	1.5.*)	;; # git config
 	1.6.*)	;; # git config
 	1.7.*)	;; # git config
+	1.8.*)  ;; # git config
 	*)	die "Unsupported version of git ($gitver)" ;;
 esac 
