Index: ctdb-1.0.114/configure.ac
===================================================================
--- ctdb-1.0.114.orig/configure.ac
+++ ctdb-1.0.114/configure.ac
@@ -8,7 +8,7 @@ AC_CONFIG_SRCDIR([server/ctdbd.c])
 
 if test "${libdir}" = '${exec_prefix}/lib'; then
   case `uname -m` in
-    x86_64|ppc64|powerpc64)
+    ppc64|powerpc64)
       libdir='${exec_prefix}/lib64'
       ;;
     *)
